---
"@module-federation/doctor": patch
---

Make the `demo` policy pack hide only local development noise from bare remote
entries and version-first offline-remotes while keeping external and CI
findings visible.
