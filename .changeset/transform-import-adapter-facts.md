---
"@tonoizer/mfdoctor": minor
---

Collect public `source.transformImport` library names from the Modern.js and Rsbuild adapters so `config/transform-import-share-conflict` can fire on antd/arco-style overlaps instead of skipping when facts are unknown. Function-form transformImport is not invoked.
