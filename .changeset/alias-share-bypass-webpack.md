---
"@tonoizer/mfdoctor": minor
---

Add `config/alias-share-bypass` to warn when webpack/rspack/rsbuild `resolve.alias` object entries overlap Module Federation `shared` keys. Function aliases stay unknown. Vite keeps `vite/alias-share-bypass`.
