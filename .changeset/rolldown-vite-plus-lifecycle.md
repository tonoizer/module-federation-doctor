---
"@module-federation/doctor": minor
---

Extend the Vite adapter for Rolldown and Vite Plus emit lifecycles: detect
flavor/engine, prefer on-disk assets with `closeBundle` fallback, and record
honest `doctor/partial-analysis` when emit facts are missing.
