---
"@tonoizer/mfdoctor": minor
---

Add the internal scoped evidence v2 rollout controller. It defaults to the legacy v1 path, requires all release gates for `v2-compat`, and supports an emergency legacy rollback switch.
