---
"@module-federation/doctor": patch
---

Document and CI-exercise the v1 compatibility matrix (Vite / Rspack / Rsbuild
supported; Webpack pending on #10; Node 22+24; pnpm primary; terminal / JSON /
SARIF on the CI path).
