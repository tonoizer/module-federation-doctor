---
"@tonoizer/mfdoctor": minor
---

Extend `mfdoctor capabilities` with agent-facing nonGoals, completeness, githubAction, networkPolicy, and a bundler matrix derived from `fixtures/compatibility-matrix.json`.
