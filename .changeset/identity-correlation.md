---
"@tonoizer/mfdoctor": minor
---

Add additive semantic identity correlation, scoped capability edges, and deterministic coverage summaries. Existing V1 reports, fingerprints, CLI output, and exit behavior are unchanged.
