---
"@tonoizer/mfdoctor": minor
---

Add `config/js-remote-without-type-urls` to warn when a host consumes types from a direct `.js` remote without `dts.consumeTypes.remoteTypeUrls`. Manifest remotes skip.
