---
"@tonoizer/mfdoctor": patch
---

Normalize the product brand to lowercase `mfdoctor` across CLI output, documentation, integrations, schemas, fixtures, and release metadata without changing the package name, CLI command, or public API identifiers.
