---
"@tonoizer/mfdoctor": patch
---

Pin workspace-federation-gate docs to a release tag, require or install the mfdoctor CLI in the Action, and fail loudly when SARIF upload lacks `security-events`.
