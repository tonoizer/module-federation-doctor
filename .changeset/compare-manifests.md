---
"@tonoizer/mfdoctor": minor
---

Add `mfdoctor compare` to diff deployed Module Federation manifests for name, exposes, shared, publicPath, and remoteEntry using the same probe network safety, with JSON and SARIF output.
