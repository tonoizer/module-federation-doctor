---
"@tonoizer/mfdoctor": minor
---

Allow `--diagnostics-dir` dumps to include more than the default top-3 agent prompts via `--diagnostics-prompts` / `diagnosticsPromptLimit` / `MFDOCTOR_DIAGNOSTICS_PROMPTS`, hard-capped at 25. Terminal top-3 output is unchanged.
