---
"@tonoizer/mfdoctor": minor
---

Preserve unknown expose object keys through normalize and canonical unknown fields. Confirmed SDK `ExposesConfig` remains `import` and `name`; extra keys are not treated as a hide API for `config/expose-key-invalid`.
