# Limitations

MVP supports Vite, direct Rspack, and Rsbuild. Webpack, Rolldown, Modern.js,
runtime diagnostics, Vite Plus, HTML analysis UI, and broad compatibility
matrices are follow-up work. Static imports cannot see every runtime import.
Doctor does not rely on undocumented private Module Federation plugin fields.
