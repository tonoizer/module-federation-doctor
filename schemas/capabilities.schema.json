{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/tonoizer/module-federation-doctor/schemas/capabilities.schema.json",
  "title": "MFDoctor CLI capabilities",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "package",
    "commands",
    "formats",
    "schemaVersions",
    "exitCodes",
    "nonInteractive",
    "schemas",
    "nonGoals",
    "completeness",
    "githubAction",
    "networkPolicy",
    "operations",
    "bundlerMatrix"
  ],
  "properties": {
    "schemaVersion": { "const": 1 },
    "package": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "version"],
      "properties": {
        "name": { "const": "@tonoizer/mfdoctor" },
        "version": { "type": "string", "minLength": 1 }
      }
    },
    "commands": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": { "pattern": "^[a-z][a-z-]*$" },
      "additionalProperties": { "$ref": "#/$defs/command" }
    },
    "formats": {
      "type": "array",
      "uniqueItems": true,
      "items": { "enum": ["terminal", "json", "sarif"] }
    },
    "schemaVersions": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": { "pattern": "^[a-z][a-z-]*$" },
      "additionalProperties": { "type": "integer", "minimum": 1 }
    },
    "exitCodes": {
      "type": "object",
      "additionalProperties": false,
      "required": ["0", "1", "2"],
      "properties": {
        "0": { "type": "string", "minLength": 1 },
        "1": { "type": "string", "minLength": 1 },
        "2": { "type": "string", "minLength": 1 }
      }
    },
    "nonInteractive": {
      "type": "object",
      "additionalProperties": false,
      "required": ["flags", "commands"],
      "properties": {
        "flags": {
          "type": "array",
          "items": { "type": "string", "pattern": "^--[a-z][a-z-]*$" },
          "uniqueItems": true
        },
        "commands": {
          "type": "object",
          "minProperties": 1,
          "propertyNames": { "pattern": "^[a-z][a-z-]*$" },
          "additionalProperties": { "type": "string", "minLength": 1 }
        }
      }
    },
    "schemas": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": { "pattern": "^[a-z][a-z-]*$" },
      "additionalProperties": {
        "type": "string",
        "pattern": "^\\./schemas/[a-z0-9.-]+\\.schema\\.json$"
      }
    },
    "nonGoals": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    },
    "completeness": {
      "type": "object",
      "additionalProperties": false,
      "required": ["check", "emit", "workspace", "probe", "runtime"],
      "properties": {
        "check": { "type": "string", "minLength": 1 },
        "emit": { "type": "string", "minLength": 1 },
        "workspace": { "type": "string", "minLength": 1 },
        "probe": { "type": "string", "minLength": 1 },
        "runtime": { "type": "string", "minLength": 1 }
      }
    },
    "githubAction": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "uses", "pinToTag"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "uses": { "type": "string", "minLength": 1 },
        "pinToTag": { "type": "string", "minLength": 1 }
      }
    },
    "networkPolicy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["offlineByDefault", "networkCommands", "probe"],
      "properties": {
        "offlineByDefault": { "type": "boolean" },
        "networkCommands": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "probe": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "httpsRequired",
            "httpAllowedForLoopbackInitialUrl",
            "ssrfProtection",
            "blockPrivateLinkLocalMetadataHosts",
            "maxRedirects",
            "defaultTimeoutMs",
            "defaultMaxBytes",
            "neverExecutesRemoteEntry",
            "rejectEmbeddedCredentials"
          ],
          "properties": {
            "httpsRequired": { "type": "boolean" },
            "httpAllowedForLoopbackInitialUrl": { "type": "boolean" },
            "ssrfProtection": { "type": "boolean" },
            "blockPrivateLinkLocalMetadataHosts": { "type": "boolean" },
            "maxRedirects": { "type": "integer", "minimum": 0 },
            "defaultTimeoutMs": { "type": "integer", "minimum": 1 },
            "defaultMaxBytes": { "type": "integer", "minimum": 1 },
            "neverExecutesRemoteEntry": { "type": "boolean" },
            "rejectEmbeddedCredentials": { "type": "boolean" }
          }
        }
      }
    },
    "operations": { "$ref": "#/$defs/operations" },
    "bundlerMatrix": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "supported", "partial", "bundlers", "localCi"],
      "properties": {
        "source": { "const": "./fixtures/compatibility-matrix.json" },
        "supported": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "partial": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "bundlers": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/bundlerEntry" }
        },
        "localCi": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/localCiCell" }
        }
      }
    }
  },
  "$defs": {
    "operations": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schemaVersion", "commands"],
      "properties": {
        "schemaVersion": { "const": 1 },
        "commands": {
          "type": "object",
          "minProperties": 1,
          "propertyNames": { "pattern": "^[a-z][a-z-]*$" },
          "additionalProperties": { "$ref": "#/$defs/operation" }
        }
      }
    },
    "operation": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "description",
        "arguments",
        "options",
        "prerequisites",
        "network",
        "writtenArtifacts",
        "errorCodes"
      ],
      "properties": {
        "description": { "type": "string", "minLength": 1 },
        "formats": {
          "type": "array",
          "uniqueItems": true,
          "items": { "enum": ["terminal", "json", "sarif"] }
        },
        "arguments": {
          "type": "array",
          "items": { "$ref": "#/$defs/argument" }
        },
        "options": {
          "type": "array",
          "items": { "$ref": "#/$defs/option" }
        },
        "prerequisites": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "network": { "$ref": "#/$defs/network" },
        "writtenArtifacts": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "errorCodes": {
          "type": "object",
          "propertyNames": { "pattern": "^[a-z][a-z0-9-]*$" },
          "additionalProperties": { "$ref": "#/$defs/errorCode" }
        }
      }
    },
    "argument": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "type", "required", "description"],
      "properties": {
        "name": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
        "type": { "enum": ["path", "glob", "url", "string", "enum"] },
        "required": { "type": "boolean" },
        "repeatable": { "type": "boolean" },
        "minimumCount": { "type": "integer", "minimum": 1 },
        "values": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "description": { "type": "string", "minLength": 1 }
      }
    },
    "option": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "type", "description"],
      "properties": {
        "name": { "type": "string", "pattern": "^--[a-z][a-z0-9-]*$" },
        "aliases": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "pattern": "^-[a-zA-Z]$" }
        },
        "type": {
          "enum": ["boolean", "integer", "string", "enum", "path", "glob", "url"]
        },
        "required": { "type": "boolean" },
        "repeatable": { "type": "boolean" },
        "values": {
          "type": "array",
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "minimum": { "type": "number" },
        "maximum": { "type": "number" },
        "description": { "type": "string", "minLength": 1 }
      }
    },
    "network": {
      "type": "object",
      "additionalProperties": false,
      "required": ["mode", "userInitiated", "behavior"],
      "properties": {
        "mode": { "enum": ["offline", "network"] },
        "userInitiated": { "type": "boolean" },
        "behavior": { "type": "string", "minLength": 1 }
      }
    },
    "errorCode": {
      "type": "object",
      "additionalProperties": false,
      "required": ["exitCode", "description"],
      "properties": {
        "exitCode": { "enum": [0, 1, 2] },
        "description": { "type": "string", "minLength": 1 }
      }
    },
    "command": {
      "type": "object",
      "additionalProperties": false,
      "required": ["description", "network"],
      "properties": {
        "description": { "type": "string", "minLength": 1 },
        "network": { "type": "boolean" },
        "formats": {
          "type": "array",
          "uniqueItems": true,
          "items": { "enum": ["terminal", "json", "sarif"] }
        }
      }
    },
    "bundlerEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "status", "adapter"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "status": { "enum": ["supported", "partial"] },
        "adapter": { "type": "string", "minLength": 1 }
      }
    },
    "localCiCell": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "bundler", "fixture"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "bundler": { "type": "string", "minLength": 1 },
        "fixture": { "type": "string", "minLength": 1 },
        "coverage": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
