# `@example/mfdoctor-bridge-policy`

In-repo example pack for Bridge advisory defaults (#140 / epic #131).

Keeps the three Bridge `info` advisories soft and documents how to extend `recommended` with Bridge-focused severity choices.
