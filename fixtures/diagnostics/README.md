# Diagnostic fixtures

Tests create isolated workspaces for partial config, object/string config forms,
scoped and deep imports, Windows paths, duplicate findings, custom rules,
severity overrides, redaction, ordering, fingerprints, and analyzer failures.
