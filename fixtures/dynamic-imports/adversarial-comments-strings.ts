// import "comment-only";
// loadShare("comment-only");
const text = "require('string-only') loadRemote('string-only')";
const template = `import("template-only")`;
void text;
void template;

import "react";

export { default } from "react-dom";
